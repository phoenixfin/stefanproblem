from test import test

if __name__ == "__main__":
    # test.compare_init(2)
    # test.compare_methods(2, ['perturbation', 'numerics'])
    # test.compare_with_init(2, 1)
    # for i in range(2):
    #     test.compare_with_init(i,2)
    # test.compare_init(0)
    # test.compare_perturbation_bound()
    test.compare_differences(1)
    # test.numerics_plot(1)